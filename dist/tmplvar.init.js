document.addEventListener('DOMContentLoaded', () => {
  // const select = document.querySelector('[name="type"]')
  // const options = Object.values(select.children[1].children)
  //
  // Array.from(select.children[1].children).forEach((option) => {
  //   select.children[1].removeChild(option)
  // })
  //
  // options.push(new Option('multifields3', 'custom_tv:multifields3'))
  // options.sort((a, b) => {
  //   return a.innerText < b.innerText ? -1 : (a.innerText > b.innerText ? 1 : 0)
  // })
  //
  // select.children[1].append(...options)
})
